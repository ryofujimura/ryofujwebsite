gunicorn==20.0.4  # If you are deploying to a production environment that uses Gunicorn
Flask==2.0.2  # Adjust this line based on the Flask version you're using
Werkzeug==2.0.2  # Make sure Werkzeug's version is compatible
